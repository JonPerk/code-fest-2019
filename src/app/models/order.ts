export interface Order {
    orderNumber: string;
    poNumber: string;
    totalCase: String;
    totalEach: String;
    submitted: string;
    estimatedDelivery: String;
    status: String;
}

export default Order;